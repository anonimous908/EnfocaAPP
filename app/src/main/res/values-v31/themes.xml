<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.EnfocaAPP" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/enfoca_background</item>
        <!-- Hides the default Android 12 splash screen icon to allow our Compose Splash Screen to show cleanly -->
        <item name="android:windowSplashScreenBackground">@color/enfoca_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
    </style>
</resources>
